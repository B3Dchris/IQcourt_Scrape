# Core dependencies
requests==2.32.3
python-dotenv==1.1.0
supabase==2.0.3

# Web scraping and browser automation
undetected-chromedriver==3.4.6
selenium==4.16.0

# Logging & CLI
# (Standard Python logging and argparse used — no extra packages needed)

# Testing
pytest==7.4.3
pytest-mock==3.12.0
